/* The collection of bindings and their values that exist at a given time is called the 'environment'
When the program starts up, this environment is not empty, it always contains bindings that are part of the language standard, and most of the time, it also has bindings that provide ways to interact with the surrounding system, e.g. the browser (read keyboard/mouse inputs, read window properties, etc...)
*/
